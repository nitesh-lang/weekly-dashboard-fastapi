node_modules/
dist/

# Encrypted dataset bundle -- regenerated by `npm run build` from BUYBOX_PASSWORD.
# Never commit it: a fresh salt/IV makes it change on every build.
public/data.enc

.env
.env.*
!.env.example
__pycache__/
*.pyc
.venv/
venv/

# Local Claude / editor state
.claude/

# Ads API state file + ad-hoc log captures
data/.ads_reports_state.json
data/*.log

# Excel scratch/lock files
~$*
