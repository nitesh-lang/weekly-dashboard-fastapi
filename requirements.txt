fastapi==0.115.6
starlette==0.41.3
uvicorn
# PINNED — this code runs in 3 environments (operator Windows, Render web,
# GH Actions cron that WRITES the snapshots). Unpinned pandas meant each
# could resolve a different version; on newer pandas astype(str) PRESERVES
# NaN instead of stringifying it, which caused two production incidents
# (AMS-Trend brand mistag; /api/insights/meta 500). Bump deliberately, all
# three environments at once, never by drift.
pandas==2.2.3
jinja2
python-multipart
openpyxl>=3.1.2
xlrd>=2.0.1
# Rust-based xlsx parser — kept alongside openpyxl.  On Linux CI runners
# pandas+openpyxl silently drops rows for some xlsx encodings that
# Windows Excel writes (see commit 805a020 + agent findings 2026-07-06),
# which cascaded into today's day-long W27 landing cycle.  Calamine
# reads the same files correctly; the ETL-side `_read_excel_safe`
# helper prefers it and falls back to openpyxl if unavailable.
python-calamine>=0.2.0
psycopg2-binary
SQLAlchemy>=2.0
python-dotenv
anthropic>=0.40.0
httpx>=0.27.0
requests>=2.31.0
itsdangerous>=2.1.0
bcrypt>=4.0.0